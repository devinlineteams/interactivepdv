import { useState, useEffect } from "react";
import { InputProps } from "@/types/searchComponentTypes";

export const searchForm = (inputs:InputProps[])=>{
   /* const [formValues, setFormValues] useState(inputs.map((input)=>input.value || ''))
    const */
}